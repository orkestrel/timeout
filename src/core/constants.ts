/**
 * Names the largest timeout duration the package accepts, `2_147_483_647` milliseconds.
 *
 * @example
 * ```ts
 * createTimeout({ ms: MAX_TIMEOUT_MS })
 * ```
 */
export const MAX_TIMEOUT_MS = 2_147_483_647
